services:
  - type: web
    name: f1-telemetry-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn f1_dashboard:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
